import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  
  typography: {
    fontFamily: 'Manrope, sans-serif',
  },
  palette: {
    background: {
      default: 'black', // Defina a cor de fundo desejada aqui
    },
  },
});

export default theme;